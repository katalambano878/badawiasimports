import StoreClientLayout from '@/components/StoreClientLayout';

export default function StoreLayout({ children }: { children: React.ReactNode }) {
  return <StoreClientLayout>{children}</StoreClientLayout>;
}
